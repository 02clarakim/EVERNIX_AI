services:
  - type: web
    name: evernix-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn src.agent_lab.api.main:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
